# Display ASCII BANNER
Initialize colorama
Initialize pyfiglet

function print_colored_banner(text, color):
    Print colored text using colorama

fig = Create Figlet object

banner_text = Render text using Figlet
Print colored banner text

# Display WELCOME message
Print welcome message

# Function to convert Celsius to Fahrenheit
function celsius_fahrenheit():
    while True:
        Prompt user for temperature in Celsius
        Convert Celsius to Fahrenheit using formula
        Print result
        Break out of the loop

# Function to convert Fahrenheit to Celsius
function fahrenheit_celsius():
    while True:
        Prompt user for temperature in Fahrenheit
        Convert Fahrenheit to Celsius using formula
        Print result
        Break out of the loop

# Exit function
function exit():
    Print exit message
    Exit the program

# Main program
if __name__ == '__main__':
    program_function = Dictionary with program functions

    while True:
        Print menu options
        Prompt user for program choice
        Get selected program function from dictionary

        if selected_program exists:
            Call selected_program function
        else:
            Print error message

        Prompt user if they want to run another program
        If answer is not 'y', break out of the loop
